import stitches from "../../stitches"

const MaxContainer = stitches.styled("div", {
  maxWidth: "1440px",
  margin: "0 auto",
  width: "100%",
  minHeight: "inherit",
  height: "auto",
})

export default MaxContainer
