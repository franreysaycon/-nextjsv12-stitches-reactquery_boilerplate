import stitches from "../../stitches"

const MainContainer = stitches.styled("main", {
  position: "relative",
  padding: "0 $space$4",
  minHeight: "100vh",
  height: "auto",
})

export default MainContainer
